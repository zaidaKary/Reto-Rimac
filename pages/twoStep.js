import Base from "../components/Base";
import SecondStep from "../components/SecondStep";
const  pasoDos= () => {
    return (
    <Base children2={<SecondStep />}>
    </Base>
    );
};

export default pasoDos;